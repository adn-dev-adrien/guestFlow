- Une colonne sur `reservations` : `endOfStayComplementPaidAtDeparture` (spec `recall-unpaid-arrival-complement-at-checkout.md` règle 9bis), `0` sur toutes les lignes existantes. Elle dit si c'est le SAS départ qui a encaissé le complément de fin de séjour — lui seul peut alors le dé-payer. Miroir de `complementPaidAtArrival`. Aucun backfill : une réservation antérieure se comporte comme si le complément avait été encaissé ailleurs, ce qui est le côté prudent.
