- **Une règle de spec sans test est désormais visible — et bientôt impossible** (spec `spec-rule-coverage.md`). `node scripts/check-spec-coverage.mjs` répond enfin à « quelles règles n'ont aucun test ? » en lisant la convention déjà en place dans les commentaires (584 citations « rule N »), sans rien imposer de nouveau. Un job de CI complète la mesure par une barrière : sur une PR, une règle **ajoutée** à une spec doit être citée par un test, sinon le contrôle échoue. Aucune dette héritée n'est exigée — seul le diff est regardé. Point de départ mesuré : 2 501 règles, 474 couvertes (19 %).
