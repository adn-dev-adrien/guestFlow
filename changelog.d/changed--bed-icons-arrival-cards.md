- **Planning arrival cards: coloured bed icons instead of "SIMPLE/DOUBLE" text.** The bed breakdown on the arrival tiles (`ReservationCard`) now shows a small **front-view bed pictogram** — a **narrower** bed with **one pillow** for a single, a **wider** bed with **two pillows** for a double — in colour (wood frame, sage duvet, cream pillow), keeping the `×N` count; baby beds keep their short "BÉBÉ" label. New reusable `client/src/components/BedIcon.js` (also mirrored on the public WordPress accommodation pages). +2 test assertions.
