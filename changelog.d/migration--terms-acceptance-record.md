- **CGV tables** — new `terms_draft`, `terms_versions` (insert-only), `terms_acceptances` (deleted with their reservation); `app_settings.requireTermsAcceptance` (default **1**) and `lastSeenPluginVersion`. One-shot `terms_cgv_url_confirmation_v1` inserts `{{#if hasCgvUrl}}…{{cgvUrl}}…{{/if}}` into the stored `reservation_confirmation` template (FR + EN), before « Une question… » / « Any question… » (end of body when the operator removed it), skipped when `{{cgvUrl}}` is already there. No existing row is altered otherwise; past website requests read « aucune acceptation enregistrée ».
