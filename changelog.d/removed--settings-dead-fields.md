- **Settings with no effect** (spec `settings-rationalization.md`): the six « Délais & relances » payment timings (and `PUT /api/payments/settings` timings), the SMTP port (derived from the security mode), the school-holiday sync interval / horizon (fixed at 60 days / 24 months, `GET|PUT /api/school-holidays/sync-settings` removed), the early-arrival / late-departure rows of the options catalogue (edited on the property), and the unused `payment_methods` table.
