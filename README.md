Screenshots referenced from pull request descriptions.

This branch carries only PNG assets: it is never merged into `master`, so the
repository history stays free of binaries while PR bodies can still render
before/after images.
